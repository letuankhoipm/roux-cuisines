import React from "react";
import { Route, Routes } from "react-router-dom";

const AdminRouters: React.FC<any> = () => {
  return <Routes>{/* <Route path="/" element={<Home />}></Route> */}</Routes>;
};

export default AdminRouters;
